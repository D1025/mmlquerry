# --- Postgres ---
POSTGRES_USER=mizar
POSTGRES_PASSWORD=changeme
POSTGRES_DB=mizar

# --- MinIO (S3) ---
MINIO_ROOT_USER=mizar_minio
MINIO_ROOT_PASSWORD=supersecret
S3_BUCKET=mizar-esx
S3_REGION=us-east-1

# --- Aplikacja ---
APP_PORT=8080
ESX_SOURCES=https://raw.githubusercontent.com/arturkornilowicz/esx_files/main/esx_mml/Miz_0.esx
POLL_CRON=0 0/15 * * * *   # co 15 min
HTTP_TIMEOUT_MS=15000
ADMIN_PASSWORD=changeme-admin

# --- Frontend / Nginx ---
FRONTEND_API_BASE_URL=/api
# Publiczny IP serwera  dla certyfikatu Let's Encrypt.
LETSENCRYPT_IP=159.89.106.226
LETSENCRYPT_EMAIL=admin@example.com
CERTBOT_RENEW_INTERVAL_SECONDS=43200
NGINX_RELOAD_CHECK_SECONDS=30


# GitHub Releases (repo owner/name)
GITHUB_REPO=arturkornilowicz/esx_files
# opcjonalnie: podnosi limit do 5000 req/h i włącza „free” 304 na ETag
GITHUB_TOKEN=
# tryb ingestu: DIRECT (Twoja lista URL-i) albo GITHUB_RELEASES
ESX_MODE=GITHUB_RELEASES
